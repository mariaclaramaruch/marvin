function vogalOuConsoante(letra) {
    var tipo = letra.toLowerCase()
    if (tipo == "a"|| tipo == "e" || tipo == "i" || tipo == "o" || tipo == "u") {
    return ("Vogal")
    } else { 
        return ("Consoante")
}
}
